---
import { featureData, sectionHeaders } from "../data";
---
<div id="features" class="container mx-auto scroll-instant lg" >
    <div class="grid items-center grid-cols-1 pb-8 lg:grid-cols-2 md:grid-cols-2">
        <div>
            <h6 class="mb-2 text-base font-medium text-green-600 uppercase">{sectionHeaders.feature.title}</h6>
            <h3 class="mb-4 text-xl font-semibold md:text-2xl dark:text-white md:mb-0">{sectionHeaders.feature.subtitle} <br></h3>
        </div>
        <div>
            <p class="max-w-xl text-slate-400">{sectionHeaders.feature.text}</p>
        </div>
    </div>

    <div class="grid grid-cols-1 gap-6 lg:grid-cols-3 md:grid-cols-2">
        {featureData.map((item, index)=>( 
             <div class="group p-6 mt-8 transition-all duration-500 features hover:shadow-xl hover:shadow-slate-100 dark:hover:shadow-slate-800 rounded-3xl bg-white dark:bg-slate-900 border border-gray-100 dark:border-slate-700 hover:border-green-200 dark:hover:border-green-700 hover:-translate-y-2" transition:name={item.slug + '-card'}>
                
                <!-- Icon with improved styling -->
                <div class="relative mb-6">
                  <div
                    class="flex items-center justify-center text-3xl text-green-600 align-middle shadow-sm size-20 bg-gradient-to-br from-green-50 to-green-100 dark:from-green-900 dark:to-green-800 rounded-xl group-hover:shadow-lg group-hover:scale-110 transition-all duration-300"
                    transition:name={`icon-${item.slug}`}
                  >
                    <i class={item.icon}></i>
                  </div>
                  
                  <!-- Badge for special services -->
                  {index < 2 && (
                    <div class="absolute -top-2 -right-2 bg-green-500 text-white text-xs px-2 py-1 rounded-full">
                      Top
                    </div>
                  )}
                </div>

                <div class="content">
                    <a href={`/features/${item.slug}`} class="text-xl font-semibold transition-all duration-500 ease-in-out hover:text-green-600 dark:text-white dark:hover:text-green-500 group-hover:text-green-600 dark:group-hover:text-green-400">
                      {item.title}
                    </a>
                    <p class="mt-3 text-slate-400 group-hover:text-slate-500 dark:group-hover:text-slate-300 transition-colors duration-300">
                      {item.desc}
                    </p>
                    
                    <!-- Enhanced call-to-action -->
                    <div class="mt-6 pt-4 border-t border-gray-100 dark:border-slate-700">
                        <a href={`/features/${item.slug}`} class="inline-flex items-center space-x-2 text-green-600 hover:text-green-700 dark:text-green-400 dark:hover:text-green-300 font-medium transition-all duration-300 group-hover:translate-x-1">
                          <span>{item.buttonLabel}</span>
                          <i class="uil uil-arrow-right transition-transform duration-300 group-hover:translate-x-1"></i>
                        </a>
                    </div>

                    <!-- Service highlights -->
                    <div class="mt-4 flex items-center space-x-4 text-sm text-gray-500 dark:text-gray-400">
                      <div class="flex items-center space-x-1">
                        <i class="uil uil-check-circle text-green-500"></i>
                        <span>Professionell</span>
                      </div>
                      <div class="flex items-center space-x-1">
                        <i class="uil uil-clock text-blue-500"></i>
                        <span>Schnell</span>
                      </div>
                    </div>
                </div>
            </div>
        ))}
    </div>

    <!-- Call-to-Action Section -->
    <div class="mt-16 p-8 bg-gradient-to-r from-green-50 to-blue-50 dark:from-green-900 dark:to-blue-900 rounded-2xl text-center">
      <h4 class="text-2xl font-semibold text-gray-800 dark:text-white mb-4">
        Haben Sie Fragen zu unseren Leistungen?
      </h4>
      <p class="text-gray-600 dark:text-gray-300 mb-6 max-w-2xl mx-auto">
        Unser erfahrenes Team berät Sie gerne persönlich und erstellt Ihnen einen kostenlosen Kostenvoranschlag.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="tel:+4915757057121" class="inline-flex items-center justify-center px-6 py-3 bg-green-600 hover:bg-green-700 text-white font-medium rounded-lg transition-colors duration-200 space-x-2">
          <i class="uil uil-phone"></i>
          <span>Jetzt anrufen</span>
        </a>
        <a href="#contact" class="inline-flex items-center justify-center px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg transition-colors duration-200 space-x-2">
          <i class="uil uil-envelope"></i>
          <span>Kontakt aufnehmen</span>
        </a>
      </div>
    </div>
</div>